# Home-Sidechain Trigger v30

UI/interaction polish checkpoint based on v29.

Changes:
- Subtle NOW playhead and current-level ghost on the waveform scope.
- Threshold interaction is limited to an 18 px band around the actual threshold line instead of the whole graph.
- Shift-drag provides fine threshold adjustment.
- A/B/C link selector uses a subtle active underline/focus treatment.
- Cool Down value is integrated as clean right-aligned text; surrounding card remains non-interactive.
- Cool Down slider mapping and hit area remain parameter-accurate.
- Existing trigger processing, MIDI, Home-Link, persistent trigger highlight, and compact 600 x 365 layout are preserved.
